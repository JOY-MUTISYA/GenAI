# Example environment file for the ima_prompt project.
# Copy this to `.env` and fill in real values for local development.

# OpenAI API key (keep this secret)
OPENAI_API_KEY=your-openai-api-key-here

# Optional: provider selection or other settings
#MODEL_NAME=openai/gpt-4o
#API_BASE=https://api.openai.com

# Notes:
# - Never commit your real `.env` to source control. Add `.env` to `.gitignore`.
# - For CI, use the platform's secure secret store instead of a plain `.env` file.
